# HANDOFF — 2026-07-19 (DB cleanup + hotel category + แผน social + working agreements)

> Session นี้ **ไม่ได้แตะ frontend/backend code เลย** · เป็น session ฝั่ง **data + strategy + วิธีทำงาน**
> ต่อจาก `HANDOFF_2026-07-18.md` (UI overhaul → pilot-ready)

---

## 🔴 ด่วนที่สุด — ทำก่อนเริ่มโรงแรมพรุ่งนี้

### Render หลับ → cron ตายทั้งหมด (ยังไม่แก้)

**ยืนยันแล้วจาก Render dashboard:**
- แบนเนอร์: *"Your free instance will spin down with inactivity"*
- ค้น log `[check_expired_jobs]` ย้อน **7 วัน → No matching logs** = **cron ไม่รันเลยแม้แต่ครั้งเดียว**

**สาเหตุ:** Render free tier หลับหลังไม่มี traffic ~15 นาที → process ดับ → APScheduler (รันใน process เดียวกับแอป) ดับตาม · interval 30 นาทีไม่มีทางครบรอบ

**ผลกระทบ — ตายหมด 4 ตัว (main.py บรรทัด 626-630):**
| Cron | ผลถ้าไม่รัน |
|---|---|
| `check_noshow_workers` (5 นาที) | **no-show detection ตาย** ← โรงแรมแคร์สุด, เป็นจุดขาย anti-ghosting |
| `auto_verify_completed_jobs` (30 นาที) | งาน completed ไม่ auto-verify |
| `check_expired_jobs` (30 นาที) | งานค้างเกิน 48 ชม. ไม่ปิด (บัคที่พี่เจอ) |
| `send_d1_reminders` (11:00 UTC) | ไม่เตือน worker ล่วงหน้า 1 วัน |

**⚠️ โค้ด auto-close ถูกต้องอยู่แล้ว — ไม่ต้องแก้โค้ด** ปัญหาอยู่ที่ infra ล้วน

**วิธีแก้ (เลือก 1):**
1. ⭐ **Render Starter $7/mo** — กด upgrade ใน dashboard, always-on, ไม่ต้องแตะโค้ด · **แนะนำ** (ตัดสินใจแล้วว่าจะทำ ยังไม่ได้ทำ)
2. ฟรี: UptimeRobot / cron-job.org ping `/health` ทุก 5 นาที (มีเขียนไว้ใน `launch/REVISED_PLAN.md` Phase 0 ข้อ 1 แล้ว)

**หลังแก้ → verify:** ค้น log `[check_expired_jobs]` ควรเห็น `done closed=N` ทุก 30 นาที

> **ทำไมด่วน:** ถ้าโรงแรมเริ่มโพสต์งานจริงพรุ่งนี้แล้ว worker ไม่มา — ระบบจะ**ไม่ detect no-show** เลย = ฟีเจอร์ที่ใช้ขายโรงแรมเงียบสนิท

---

## ✅ ที่ทำเสร็จใน session นี้

### 1. ล้าง test data ออกจาก production DB
- **85 → 5 users** (ลบ 80) · employers 32→5 · workers 36→1 · jobs 50→5 · apps 44→4 · noti 164→24
- ลบทิ้ง: `Test Corp` ×10, `DispCorp` ×8, `DH Corp` ×2, `QA Co` ×2, `Repro Corp`, + dev throwaway (example.com/a.com/gx.com/ex.com)
- **เก็บไว้ 5:** `emp2@wehire.com` (เจ๊แก้กับข้าว) · `Nattanin999@hotmail.com` (ณัฏฐนิน) · `a1234@e1.com` (สวนมังคุด) · `manita110446@gmail.com` (eiei) · `vi.nutthaphat@gmail.com` (พี่ — employer ABC + worker)
- ทำใน **atomic transaction** + **dump JSON backup ก่อนลบ**: `_deleted_backup_20260718_094728.json` (80 users + 45 jobs)
- ⚠️ **backup มี PII + password hash → ห้าม commit** · gitignore แล้ว

### 2. เพิ่มหมวดงานโรงแรม (migration 026 — apply แล้ว)
- ไฟล์: `026_hotel_category.sql` · commit `839468d`
- หมวด `hotel` "โรงแรมและที่พัก" · **icon เว้นว่าง `''` ไม่ใช้ emoji** (ไม่ใช่ NULL — frontend render `${c.icon}` จะโชว์คำว่า "null")
- **9 titles:** `room_attendant` (housekeeping) · `laundry_attendant` · `bellboy` · `concierge` · `doorman` · `room_service` · `steward` · `banquet_staff` · `pool_attendant`
- **`maid` + `gardener` reuse จากหมวด cleaning** (ไม่ทำซ้ำ) — เคยจะลบ `maid` แต่**ยกเลิก** เพราะใช้กับงานที่พักอาศัย/ทั่วไปได้ · maid ยังอยู่ครบ
- ไม่ต้อง deploy (data ล้วน) แต่ client cache หมวดไว้ 1 session → **hard-refresh** ถึงเห็น

> **ข้อจำกัดที่เจอ:** SVG ใส่ใน `<option>` ของ `<select>` **ไม่ได้** (browser รับแค่ plain text) → นี่คือเหตุผลที่ 8 หมวดเดิมยังเป็น emoji · ถ้าอยากได้ไอคอน SVG ต้องเปลี่ยน category picker เป็น chip grid (ยังไม่ทำ — พี่เลือกไม่เอาไอคอน)

### 3. `.gitignore` — กัน PII หลุด
- เพิ่ม `_*.py`, `_deleted_backup_*.json` · commit `0159723`
- ยืนยันแล้วว่า temp scripts + backup ถูก ignore ครบ

### 4. แผน Social & Content — `launch/06_SOCIAL_CONTENT_PLAN.md` (ไฟล์ใหม่)
ต่อจาก `GTM_traction_plan.md` + `REVISED_PLAN.md` + `02_WORKER_RECRUIT.md`

---

## 🎯 นิยาม + เป้าที่ล็อกแล้ว (สำคัญ — เคยเข้าใจผิดมาก่อน)

> **1 traction = โรงแรม 1 โรงที่ repeat** · repeat กี่ครั้งก็นับ **1** · **20 traction = โรงแรม 20 โรงที่ repeat** (ไม่ใช่ 20 pairs, ไม่ใช่ repeat ลึกเจ้าเดียว)

**บันได:**
```
โรงแรมในมือเท่านั้น → โรงแรม repeat โรงแรก (พิสูจน์ wedge)
   ↓
5–8 โรง repeat  ═══ MINIMUM-PITCHABLE → เริ่มเดินคุย Angel
   ↓
20–30 โรง repeat ═══ TRACTION แข็ง (breadth + retention)
   ↓ Gate 2
ขยายตลาดอื่น (TikTok/viral, หมวด/โซนใหม่)
```

**เลขอย่างเดียวไม่พอ** — ต้องมาคู่กับ **velocity (กี่โรง/เดือน) + unit econ (fee 6% เก็บจริง, GMV/โรง) + wedge story (ทำไม repeat)**

**🚧 Guardrails ช่วงนี้ (non-negotiable):**
- ❌ TikTok = plan เฉยๆ **ยังไม่ทำ** (รอ Gate 2 · เก็บ stock คลิปได้ ไม่ยิง)
- ❌ ไม่ออกนอก belt (Sukhumvit / เพชรบุรีตัดใหม่ / วิทยุ / รัชดา / ห้วยขวาง / ดินแดง)
- ❌ **โรงแรมล้วนๆ** ไม่แตะ F&B / factory / หมวดอื่น
- ✅ social ตอนนี้ = FB Page + กลุ่มเฉพาะ + LINE **ดึง worker ป้อน belt** เท่านั้น

---

## 🤝 Working Agreements ที่ตกลงใหม่ (ใส่ CLAUDE.md แล้ว)

| กฎ | รายละเอียด |
|---|---|
| **prod DB write** | **เพิ่มของ (INSERT reference data)** → Claude รันได้ · **DELETE / UPDATE ข้อมูลจริง / ALTER / DROP** → **ส่ง SQL ให้พี่รันใน Supabase เอง** |
| **หลัง compact** | Claude ต้อง **state-check** — สรุปตัวเลข/สถานะ/นิยามสำคัญให้ตรวจก่อนทำงานต่อ (รายละเอียดเสื่อมก่อนหลักการ) |
| **รู้จริง vs เดา** | Claude ต้องแยกให้ชัดว่าอันไหน grounded อันไหน speculation — ห้ามขายการเดาเป็น insight |
| **เสนอทางเลือก** | ของสำคัญให้เสนอ **2-3 ทาง + tradeoff** ไม่ใช่คำตอบเดียว (กันพี่ติดโหมด "อนุมัติ") |
| **bandwidth** | ถ้าพี่กำลังทำงานคู่ขนาน (เช่นทำ mockup ไปด้วย) → Claude **ยื่น SQL ให้พี่รันเอง** ไม่เสนอรันให้ |

> **เหตุผลเบื้องหลัง:** session ยาวต่อเนื่อง = context เสื่อม (พี่รับ trade-off นี้เพื่อความต่อเนื่อง เพราะ handoff เขียนไม่ครบเส้นทางการคิด) · สิ่งที่เสื่อมก่อนคือ**รายละเอียด/ตัวเลข** ส่วน**หลักการอยู่ทน** → กลไกจับ drift จึงต้องเป็นระบบ ไม่ใช่ความจำ

---

## 📋 ค้างอยู่ (carry forward)

**ก่อน pilot จริง:**
- [ ] 🔴 **Render Starter $7** (หรือ keep-alive ping) — cron ตายอยู่
- [ ] Rotate GitHub token (โผล่ plaintext ใน git remote)
- [ ] hard-refresh เช็คว่าหมวด "โรงแรมและที่พัก" ขึ้นใน dropdown

**Parked (ไม่ block):**
- Escrow / Safety / Pro-rata / OT → Phase 3 (design เคาะแล้วใน HANDOFF 07-18)
- noti ชนกัน (P2)
- bottom nav mobile
- `018_job_categories_v2.sql` = **ON HOLD** เปิดหมวดใหม่เมื่อมี supply เท่านั้น
- เพิ่ม `landing.html` ใน workflow trigger paths (token ไม่มี workflow scope)

**พร้อมทำต่อเมื่อสั่ง:**
- Content calendar 30 วัน
- FB Page cover (Canva)
- Pitch: traction slide + อัปเดต `05_ANGEL_ONEPAGER.md` ให้ผูก 5-8 โรง + velocity + wedge
- Pitch เวอร์ชันไทย/อังกฤษ ไว้ซ้อม (อังกฤษ = เตรียมสำหรับ Series A ไม่ใช่ตอนนี้)

---

## 🔑 Key context สำหรับ session หน้า
- DB ตอนนี้ **สะอาด** เหลือ 5 accounts จริง · **supply = worker 1 คน** (ของพี่เอง) → หน้า matching จะว่างจนกว่า worker จริงเข้ามา = ปกติ ไม่ใช่บัค
- Backend Render (`we-re-hire.onrender.com`) · Frontend Cloudflare Workers · Supabase Tokyo
- Frontend = 2 ไฟล์ (`landing.html` + `index.html` → bundle เป็น `worker.js`) · แก้ landing เดี่ยวๆ ไม่ deploy ต้องแตะ `wrangler.toml` ด้วย
- Migration ล่าสุด = **026** (025 = employer_province)
