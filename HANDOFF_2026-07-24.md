# HANDOFF — 2026-07-24 (Cron postmortem + Onboarding UX + Security audit + RN prep)

> Session ยาวข้ามหลายวัน (19–24 ก.ค.) ต่อจาก `HANDOFF_2026-07-19.md`
> ครอบทั้ง frontend UX, infra postmortem, security audit, PDPA, และ prep งาน React Native (ยังไม่เริ่มจริง)

---

## ✅ ที่ทำเสร็จใน session นี้

### 1. Navigation tooling — กัน Claude อ่านไฟล์ใหญ่มั่ว
- `tools/gen_map.py` (ใหม่) — auto-generate `INDEX_MAP.md` จาก `index.html` (169 functions, 19 pages, 62 endpoints, sha256 check ผ่าน `--check`)
- `COUPLING_MAP.md` (ใหม่) — 17 กับดัก hidden-coupling ที่เจอจริงระหว่างแก้โค้ด (router sync, timer cleanup, GPS-not-stored-by-design, skill picker 2 โหมด ฯลฯ)
- รันซ้ำทุกครั้งที่แก้ `index.html` — ต้อง `python3 tools/gen_map.py` หลังแก้ไฟล์เสมอ

### 2. Onboarding UX — soft-gate checklist (จากฟีดแบ็กผู้ทดสอบจริง 2 คน)
- **Worker:** `workerChecklist()` + `canApplyNow()` + apply-gate modal (`showProfileGateModal`) แทน error ดิบ · `_pendingApplyJobId` ย้ายไป `sessionStorage` (รอด F5) · checklist card บน dashboard · onboarding modal เด้งครั้งเดียว (`localStorage` flag)
- **Employer:** ลบฟอร์มสร้างโปรไฟล์ซ้ำที่ไม่มาตรฐาน (ไม่เก็บเบอร์/lat-lng) → route เดียวผ่าน `.ep-form` · checklist `eChk` (3 ข้อ) แทน `verifyNudge` เดิม · ปุ่ม "ขอ Verify" กลับมาทำงานถูกต้อง (ต้องเป็น `<button>` จริง ไม่ใช่ลิงก์)
- **Multi-skill fix:** `syncSkillCode()` เดิม overwrite skill เก่าเวลาแก้โปรไฟล์ (บั๊ก) → แก้เป็น accumulate สูงสุด 3 + chip UI + สี X แดงตาม feedback
- **Label:** skill code (`loader`, `general_repair`) → แปลไทยบน profile card ผ่าน `ensureTitleLabels()`
- **Avatar:** profile/workplace photo + fallback initials (worker/employer dashboard, active shift)

### 3. Cron postmortem — แก้ misdiagnosis เดิม (สำคัญ อ่านก่อนแตะ cron อีก)
เดิมเข้าใจผิดว่า "Render free tier หลับ → cron ตาย" ทั้งที่หลักฐาน ("ไม่มี log") ใช้ไม่ได้ตั้งแต่แรก
**สาเหตุจริง 3 บั๊กซ้อน:** logger ไม่มี handler (info ถูกทิ้งเงียบ) · `/health` ไม่รับ HEAD (UptimeRobot false-Down) · misfire_grace_time default 1 วิ
**แก้ครบ + ยืนยันด้วย log จริง:** 171 รอบต่อเนื่อง 14.2 ชม. ไม่มีรอบขาด — **Render Starter $7 ไม่จำเป็นตอนนี้**
รายละเอียดเต็ม + เงื่อนไขห้ามถอด → `CLAUDE.md` หัวข้อ 5

### 4. PDPA / Consent
- `POLICY_VERSION` refactor: จาก hardcode `"1.0"` 2 จุด → constant เดียว (`main.py`)
- Design เคาะแล้ว (ยังไม่ implement): consent modal แบบเปิด + tickbox อิสระ + สรุปภาษาชาวบ้าน (เลิกแนวคิด forced-scroll)
- Draft copy สำหรับ location consent (ตอนเปิดปุ่ม "รับงาน") → `policies/02_PRIVACY_POLICY_PDPA.md` §9.1 (draft — ยังไม่ ship ฟีเจอร์จริง)

### 5. Security Audit รอบ 2 — `security_audit_2026-07-22.md`
ตรวจ auth/JWT, SQL injection (217 query), IDOR (18 endpoint), XSS (64 จุด), upload — **ไม่มี Critical**
**แก้แล้ว 2/5:** CORS ตัด `"null"` origin ออก (default + runtime filter) · admin secret เทียบด้วย `secrets.compare_digest` (timing-safe)
**ยังไม่แก้ (🟡 ต่ำ ไม่ block pilot):** rate-limit register/upload · generic error message (ไม่เผย exception ดิบ)
**🔴 ค้าง:** revoke GitHub token เก่า `ghp_9Qiy…` — พี่รับว่าจะทำเอง ยังไม่ยืนยันว่าทำแล้ว

### 6. Bridge / Sub-agent — ทดสอบ pattern ใหม่สำเร็จ
ลองใช้ Claude Code (Sonnet) ผ่าน bridge แบบ **read-only** วิเคราะห์ `index.html` เตรียมย้าย React Native (Phase 3) → ได้ `RN_MIGRATION_MAP.md` (523 บรรทัด, แยก logic reuse ได้ / DOM ต้องเขียนใหม่ / 62 endpoints / global state redesign / 8 บั๊กที่ต้องแก้ก่อน migrate)
**Verify แล้ว:** ไม่แตะ `index.html`/`main.py` เลย ไม่มี commit ที่ไม่ได้รับอนุญาต — constraint ทำงานตามที่สั่ง
**Quick win ที่เจอจาก map:** earnings screen ไม่โหลดข้อมูล (`loadEarnings()` ไม่ได้อยู่ใน `showPage()`) — แก้ web ได้เลย 1 บรรทัด ยังไม่ได้แก้

### 7. เอกสารใหม่
- `PARKED_gate2.md` — ของที่คิดไว้แล้วแต่ยังไม่ถึงเวลา (guided tour, bulk hire, auto-dispatch, grid-vs-map, GPS toggle, factory/CLMV) ทุกข้อมี "เปิดเมื่อ" กำกับชัด
- `DECISION_availability_gps.md` — ดีไซน์ availability toggle (Option B: คุมแค่ offer ใหม่ ไม่ยกเลิกงานที่รับแล้ว)
- `launch/07_HR_CALL_CHECKLIST.md` — สคริปต์โทร HR หา 3 ตัวเลข (cost/worker, จำนวนที่เรียกต่อครั้ง, lead time)
- `launch/08_USER_TEST_SHEET.html` — ใบสังเกตการณ์ user test พิมพ์ได้ (แก้คำจาก "แก/แม่" → "ผู้ทดสอบ" ให้ใช้ซ้ำได้กับทุกคน)

---

## 🔴 ต้องทำ (founder เท่านั้น)

1. **Rotate GitHub token** `ghp_9Qiy…` — ยัง live, เคยโผล่ plaintext
2. **โทร HR โรงแรม** ตาม `launch/07_HR_CALL_CHECKLIST.md` → ปลดล็อกว่าต้องทำ bulk-hire ไหม
3. **ให้ผู้ทดสอบ (แม่ หรือคนอื่น) เทสจริง** ตาม `launch/08_USER_TEST_SHEET.html` → ปลดล็อก guided tour / grid-vs-map decision ใน `PARKED_gate2.md`
4. Commit `RN_MIGRATION_MAP.md` + `launch/08_USER_TEST_SHEET.html` — **ทำแล้ว 24 ก.ค. โดยพี่เอง** (mcp_shell tool เจอปัญหาไม่ทำงานใน session ฝั่ง Claude รอบนี้)

---

## 📋 Parked / ค้างแบบไม่ block

ดูรายละเอียดเต็ม + เงื่อนไข "เปิดเมื่อ" ทั้งหมดใน **`PARKED_gate2.md`** — ไม่ copy ซ้ำที่นี่กันไฟล์ drift

สรุปสั้น: guided tour (รอผลเทสผู้ใช้จริง) · bulk hire (รอ HR call ตอบ ≥10 คน/ครั้ง) · auto-dispatch (รอ liquidity) · grid-vs-map (รอผลเทส) · availability toggle+GPS (design เสร็จ รอ consent modal) · factory/CLMV (รอ Gate 2 traction) · escrow/NDID/RN/HH (phase ถัดไป)

---

## 🔑 Key context สำหรับ session หน้า

- **Render/cron ปกติดี** — อย่าเข้าใจผิดซ้ำว่า "Render หลับ" ยืนยันด้วย log 171 รอบแล้ว เงื่อนไขที่ห้ามถอด: logger StreamHandler+INFO, UptimeRobot ping `/health` ≤5 นาที, `/health` รับ HEAD
- **Security audit 2 เสร็จ** — 2/5 fixed, เหลือแค่ 🟡 ต่ำ (ไม่ block pilot) ไฟล์เต็ม `security_audit_2026-07-22.md`
- **Bridge/sub-agent pattern ใช้ได้จริง** — read-only ปลอดภัย ลองต่อยอดได้ถ้าต้องการวิเคราะห์โค้ดแบบไม่แตะไฟล์จริง
- **⚠️ mcp_shell tool ล่มใน session 24 ก.ค.** (สั่งอะไรไปก็ไม่มีผลจริงบนเครื่อง, คืนค่า null ตลอด) — ถ้าเจออีก ให้พี่รัน git command เองแทนรอ Claude
- **Working agreements (19 ก.ค.)** อยู่ท้าย `CLAUDE.md` — DB write แยกตามย้อนกลับได้ / commit ค้างต้องยื่นให้พี่รันเองถ้าพี่ทำงานคู่ขนาน / state-check หลัง compact / แยกรู้จริง-เดาให้ชัด / เสนอ 2-3 ทางเลือกของสำคัญ
- **Quick win ที่ยังไม่ทำ:** earnings-screen routing bug (1 บรรทัดใน `showPage()`) — เจอจาก RN_MIGRATION_MAP.md แต่ยังไม่ได้แก้ที่ web

---

## 🗂️ ไฟล์ใหม่ session นี้
`tools/gen_map.py` · `INDEX_MAP.md` · `COUPLING_MAP.md` · `security_audit_2026-07-22.md` · `PARKED_gate2.md` · `DECISION_availability_gps.md` · `launch/07_HR_CALL_CHECKLIST.md` · `launch/08_USER_TEST_SHEET.html` · `RN_MIGRATION_MAP.md`
