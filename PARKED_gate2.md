# PARKED — ของที่คิดไว้แล้วแต่ยังไม่ถึงเวลา

> ที่พักของไอเดียที่ **โจทย์ถูก แต่ยังไม่ใช่ตอนนี้** · เขียนลงแล้วสมองปล่อยได้ ไม่ต้องแบก
> สร้าง 2026-07-22 · ทุกข้อมี **"เปิดเมื่อ"** กำกับ — ถ้าไม่มีเงื่อนไข = ห้ามหยิบมาทำ

---

## 🚦 Gate ปัจจุบันอยู่ตรงไหน

```
worker 1 คน (ของพี่เอง) · โรงแรม repeat 0
        ↓
[ ตอนนี้ ] โรงแรมในมือ → repeat โรงแรก (พิสูจน์ wedge)
        ↓
5–8 โรง repeat = MINIMUM-PITCHABLE → เดินคุย Angel
        ↓
20–30 โรง repeat = TRACTION แข็ง ═══ Gate 2 ═══ ← ของด้านล่างเปิดที่นี่
```

**guardrail ช่วงนี้ (พี่ตั้งเอง):** โรงแรมล้วน · ในเบลท์ · ไม่แตะหมวดอื่น · TikTok ยังไม่ยิง

---

## 📦 รายการ Parked

### 1. Guided tour / product walkthrough (onboarding แบบเกม)
**โจทย์:** สมัครเสร็จไม่รู้จะเริ่มตรงไหน → มือชี้ทีละขั้นเหมือน Duolingo/Candy Crush

**เปิดเมื่อ:** หลังแม่ (+ คนทดสอบ low-tech) ลองจริง **แล้วสะดุดหลายจุดกระจาย**
- แม่ทำได้เองผ่าน checklist → tour ไม่จำเป็น · park ถาวร
- สะดุดจุดเดิมซ้ำ → แก้ *จุดนั้น* ให้ชัด (ถูกกว่า tour)
- สะดุดหลายจุด → tour ถึงคุ้ม
- 📏 ตัววัด: ข้อ "นับจำนวนครั้งที่ต้องอธิบาย" ใน `launch/08_USER_TEST_SHEET.html`

**ทำไมใหญ่กว่าที่คิด (ตอบคำถามพี่ 22 ก.ค.):**
tour ผูกกับ **ตำแหน่งปุ่มจริง** ("กดปุ่มนี้ → ปุ่มถัดไปโผล่ตรงนั้น")
⇒ ขยับ UI เมื่อไหร่ tour ชี้ผิดเงียบๆ · วันนี้วันเดียวเราขยับ UI 15+ รอบ
⇒ ไม่ใช่ "ทำรอบเดียวจบ" แต่ "ทำรอบเดียว แล้วซ่อมทุกครั้งที่แตะ UI"

**⚠️ กับดัก:** ถ้าต้องมี tour มาชี้ = ยอมรับว่า UI ตรงนั้นไม่ชัดพอ · tour มักกลายเป็นผ้าพันแผลปิดดีไซน์ที่งง แทนที่จะแก้ให้ไม่งง
**ตอนนี้ checklist = 80% ของประโยชน์ ที่ 5% ของต้นทุน** และไม่พังเวลาขยับ UI

---

### 2. Bulk hire (จ้างทีเดียวหลายคน)
**โจทย์:** โรงแรม/โรงงานเรียกทีละ 10-15 คน · agent รับ "โทรสายเดียวจบ" · ตอนนี้ WeHire จ้างทีละใบ (`decide` ทีละคน · `jobSlots` default 1)

**เปิดเมื่อ:** HR โรงแรมตอบว่าเรียก **≥ 10 คน/ครั้ง** (คำถามข้อ 2 ใน `launch/07_HR_CALL_CHECKLIST.md`)
- ตอบ 2-3 คน → ข้ามไปเลย ไม่คุ้มทำ

**ต้องระวัง:** จ้าง 12 คนพร้อมกัน = **atomic transaction** (`qa_slot_race.py` เตือนไว้แล้ว) ไม่ใช่ loop เรียก endpoint เดิม 12 รอบ ไม่งั้น `slots_filled` แข่งกันเกินโควตา

---

### 3. Auto-dispatch (ระบบยิงงานหาคนแทน browse)
**โจทย์:** agent = โรงแรมโทรบอก "12 คน พรุ่งนี้ 6 โมง จบ" · ระบบควรทำแบบเดียวได้ถูกกว่า — โพสต์ "ต้องการ 12" → ระบบเลือกเอง (match_score + ระยะ + ว่างจริง + ไม่ชนเวลา) → ยิงข้อเสนอ → ไม่ครบยิงชั้นถัดไป → เต็มโดย HR ไม่ต้องกดคัดเอง

**เปิดเมื่อ:** มี liquidity จริง (worker หนาแน่นในเบลท์)
**ทำไมต้องรอ:** dispatch มีคำถามที่ตอบไม่ได้ถ้าไม่มีคนจริง — ใครได้ข้อเสนอก่อน (fairness) · offer หมดอายุกี่ชม. · ปฏิเสธรัวๆ แล้วไง · คนเก่งกินงานหมดไหม

**ชิ้นส่วนมีเกือบครบแล้ว:** `match_score` · PostGIS radius · `job_occupied_range` · `backup_priority` · notifications · `auto_confirm` — ขาดแค่ลูป assign + ฝั่ง worker กดรับ + กติกาคิว

---

### 4. Grid card แทนแผนที่ (หน้าหางาน)
**โจทย์:** คนรีวิว + คนทดสอบบอกแผนที่ "งง ไม่ impact" · เสนอ grid แบบ Shopee

**เปิดเมื่อ:** ผู้ใช้จริงจากเบลท์ (โดยเฉพาะแม่) ลองแล้วบอกว่าหางานจากแผนที่ไม่เจอ
- 📏 ตัววัด: ข้อ 3.7 ใน `launch/08` — "ถ้าไม่มีแผนที่ มีแต่รายการงาน จะง่ายขึ้นหรือยากขึ้น"
- ข้อ 3.3 — "แม่ลองแตะหมุดไหม" ถ้าไม่แตะเลย = แผนที่ไม่ทำงานกับกลุ่มนี้

**หมายเหตุ:** proximity คือ *logic* · แผนที่คือ *การนำเสนอ* แค่แบบเดียว · Shopee ก็ location-aware แต่โชว์เป็นการ์ด · ทางที่ไม่ทิ้งของเดิม = การ์ดเป็น default (เรียงตามระยะ) + แผนที่เป็นเครื่องมือตอนตัดสินใจ · **แต่รอผู้ใช้จริงตัดสิน อย่าเชื่อคนทดสอบคนเดียว** (เขาเสนอจาก Fastwork mental model)

---

### 5. Availability toggle + GPS (ปุ่มรับงานผูกพิกัด)
**โจทย์:** `_cascade_backup_offer` กรอง `wp.location IS NOT NULL` แต่ไม่มีอะไรเขียนพิกัดเลย ⇒ worker ทุกคนหลุดจากระบบ backup

**สถานะ:** เคาะนิยามแล้ว (ทาง B) — ปุ่มคุมแค่ข้อเสนอใหม่ · งานรับแล้วจบที่ no-show
**เปิดเมื่อ:** พร้อมทำ — แต่ต้องมี **consent PDPA (ข้อ 9.1)** ก่อนเก็บพิกัด · Render ✅ ผ่านแล้ว
> รายละเอียดเต็มอยู่ใน `DECISION_availability_gps.md` — ไม่ใช่ Gate 2 · ทำได้เร็วกว่านั้นเมื่อ consent พร้อม

**งานย่อย:** ผูก GPS กับปุ่ม (~10 บรรทัด) · `location_updated_at` + กรอง 12 ชม. · แถบเตือน "ปิดรับงานอยู่" · consent modal

---

### 6. ตลาดอื่น — โรงงาน / CLMV / หมวดใหม่
**โจทย์:** โรงงานเรียกทีละร้อย · Stream 2 (work permit ฿10,000/คน margin ~7,500) ผูกกับ CLMV

**เปิดเมื่อ:** Gate 2 (โรงแรม repeat 20-30) — guardrail พี่: โรงแรมล้วนจนกว่าจะถึงตรงนั้น

**⚠️ 6 บั๊กที่ต้องแก้ก่อนรับเจ้าแรก (จะระเบิดตอน scale):**
1. `GET /jobs/{id}/candidates` **ไม่มี LIMIT** — 300 คนสมัคร = ดึงมาหมด
2. N+1 บน employer dashboard (บรรทัด ~3263) — งาน 20 ใบ = 20 request
3. `decide` ทีละใบ — จ้าง 100 คน = กด 100 ครั้ง (ซ้ำกับ bulk hire ข้อ 2)
4. slot race (`qa_slot_race.py`)
5. notification insert รัว + `_notifTimer` ไม่เคลียร์
6. index บน `job_applications(job_id, status)`
- `018_job_categories_v2.sql` = ON HOLD (เปิดหมวดใหม่เมื่อมี supply)

---

### 7. อื่นๆ ที่ design เคาะแล้ว รอ phase
- **Escrow / Wallet / Pro-rata** — Phase 3 · สูตร balance เคาะแล้วใน CLAUDE.md
- **NDID** — Phase 3.5 · ต้องเป็น NDID Member ก่อน
- **React Native (Expo)** — Phase 3 · API ทุกตัวใช้ได้เลยไม่ต้องแก้ backend
- **Headhunter (Stream 5)** — Phase 5 · ต้องมีใบอนุญาตจัดหางาน (฿5,000/2 ปี)

---

## 🔴 ไม่ใช่ Parked — ต้องทำเร็วๆ นี้ (เตือนไม่ให้ลืมปนกัน)
- **rotate GitHub token** `ghp_9Qiy…` — ยัง live + โผล่ใน session แล้ว
- **โทร HR โรงแรม** 3 ตัวเลข → `launch/07` (ปลดล็อกว่า cost เป็น wedge จริงไหม + ต้องทำ bulk hire ไหม)
- **ให้แม่ทดสอบ** → `launch/08` (ปลดล็อกว่าต้องมี guided tour + grid card ไหม)
- **Consent modal ตอนสมัคร** — เปิด policy ให้เห็น + สรุปภาษาชาวบ้านนำ (คุยเคาะแล้ว รอทำ)

---

## 🧭 เกณฑ์ตัดสินว่า "ทำตอนนี้" หรือ "park" (ที่ใช้ได้ผลทั้ง session 21 ก.ค.)
> **ย้อนกลับยาก** (เงิน/กฎหมาย/ข้อมูล/schema) → คิดไกลได้เลย ยิ่งไกลยิ่งดี
> **ย้อนกลับง่าย** (UI/feature/หมวดงาน) → สร้างตอน**มีหลักฐาน** ไม่ใช่ตอน**คิดออก**
>
> และ 3 คำถามคัด: (1) ช่วยปิด traction แรกไหม (2) ย้อนกลับยากไหม (3) ลูกค้ากลับมาเพราะมันไหม
