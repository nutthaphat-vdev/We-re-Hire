# HANDOFF — 2026-07-18 (UI overhaul → pilot-ready)

> Session ยาว: ยกเครื่อง UI ทั้งแอปให้ตรง Gemini mockups (อ่าน CSS จริง port มา) + ปิด loop 2 ฝั่ง + light theme + logo · **frontend เป็นหลัก, แตะ backend 1 จุด (pay lock)**

---

## ✅ ผลลัพธ์: pilot-ready ทั้ง employer + worker

```
Employer:  โปรไฟล์(inherit ที่อยู่) → โพสต์งาน → ดูผู้สมัคร → จ้าง → Live Roster → verify → จ่าย(lock ยอด)
Worker:    Dashboard → หางาน → สมัคร → Active Shift → เช็คอิน GPS → ทำงาน(timer) → จบ → ยืนยันรับเงิน
เงิน:      employer จ่าย (server คำนวณยอด) → worker ยืนยันรับ → paid_confirmed ✓ (ปิด loop)
```

## 📦 Commits หลัก (ทั้ง session)

| กลุ่ม | commit |
|------|--------|
| **Post-job inherit ที่อยู่** | `cd65401` (checkbox ใช้ที่ตั้งเดิม, ตัด pin/zone) |
| **Layout + Font** | `494a561` (จัดกลาง .page), `a1b8e80`/`6f8ab48` (Prompt+Inter) |
| **Emoji→SVG ทั้งแอป** | `86ef9fb` `b0a7138` `307ca69` `12f9ea6` `793ecd9` `d7bd551` `88b4a0a` — helper `ico()` ~30 ไอคอน |
| **Landing** | `b7b8dca` (ลบ 🚀, scroll-margin, spacing — ไฟล์ `landing.html` แยก) |
| **Live Roster** | `08df4b7` (6 states + timer สด + poll 20s), `dedfd66` (timer สมูท ไม่กระตุก) |
| **Pay lock** | `33d0475` (UI lock ยอด readonly), **`af4882e` (backend: server คำนวณยอด = ค่าจ้าง×วัน กันจ่ายต่ำ)** |
| **Settings** | `c49c337` (role-aware worker/employer), `ec005cc` (wire delete account) |
| **Theme** | `fa02df2` (full light) → `a2f664a` (**sepia** ครีมอุ่น สบายตา ตาม mockup), `7614b2f` (fix nearby panel) |
| **Employer Dashboard** | `52c93fe`→`996ab31`→`e0e672d` (port `employer-classic.css` → metrics + AI candidates + Quick Roster เป๊ะ mockup) |
| **Worker Dashboard** | `b2aa16e` (port `worker.css` scoped `.wdash` — earnings card + glass stats + next job + AI recommended) |
| **Active Shift (worker)** | `e61b3ea` (port `dashboard.css` shift-card, 5 states + ปุ่มยืนยันรับเงิน) |
| **Layout nav** | `a6d558f` (role/ภาษา/logout → sidebar footer, ลบ topbar), `41c01e4` (ลบ delete จาก sidebar, Live Roster ขึ้นบน) |
| **Logo** | `4590fe5` (WH box gradient เขียว→ฟ้า + W/H ซ้อน 3D ตาม mockup) |

## 🎨 หลักการที่ยึด (founder feedback สำคัญ)
- **อ่าน CSS จริงของ mockup (`dashboard.css`/`employer-classic.css`/`worker.css`) แล้ว port มา scoped** (`#page-dashboard`, `.wdash`, `#page-activeshift`) — ไม่ตีความเอง/ไม่ตัด · จะปรับก็ต่อเมื่อ wire ไม่ได้จริง (เช่น Live Tracking map → Quick Roster เพราะ PDPA)
- **helper `ico(name,size)`** = inline SVG กลาง (phosphor CDN ไม่ render ใน prod)

---

## 🔴 ต้องทำ (founder เท่านั้น)
1. **Rotate GitHub token** — โผล่ plaintext ใน git remote (`ghp_...`) · ตกลงว่า revoke ตอนงานใกล้จบ · **ตอนนี้ใกล้จบแล้ว ควรทำ** (GitHub → Settings → Developer settings → revoke → `git remote set-url origin https://github.com/nutthaphat-vdev/We-re-Hire.git`)
2. **(optional) เพิ่ม `landing.html` ใน workflow trigger paths** — token ไม่มี `workflow` scope push ไม่ได้ · ตอนนี้ผม bump wrangler.toml ให้ทุกครั้งที่แก้ landing แทน

## ⚠️ Deploy gotcha (จำไว้)
- Frontend = **2 ไฟล์**: `landing.html` (เสิร์ฟ `/`) + `index.html` (SPA, path อื่น) → bundle เข้า `worker.js`
- workflow trigger เฉพาะ `index.html`/`worker.js`/`wrangler.toml` — **แก้ landing.html เดี่ยวๆ ไม่ deploy** ต้องแตะ wrangler.toml ด้วย

## 📋 Parked (ไม่ block pilot)
- **Escrow / Safety / Pro-rata / OT** — settlement layer, Phase 3 (runway) · design เคาะแล้ว:
  - `worktime ≥ 90%` = completed จ่ายเต็ม · `< 90%` = disputed → admin
  - **Safety (เหตุสุดวิสัย):** employer กด Safety + dropdown เหตุผล (บังคับ) → `employer สั่งหยุด/อันตราย` = จ่าย 100% · `เหตุสุดวิสัย` = `<50% จ่าย 50% / ≥50% จ่าย 100%` + platform waive fee · admin review เสมอ · worker ไม่โดนตัดคะแนน · (founder เอนไปทาง "ให้ employer เลือกเอง" + park ไม่ over-design)
- **Pay validation แบบเต็ม** (pro-rata/OT ใน pay endpoint) — ทำทีเดียวกับ escrow · ตอนนี้ happy path = จ่ายเต็ม
- **noti 15 นาทีก่อนหมดกะ** (cron ใหม่)
- **bottom nav mobile** (hybrid: 4-5 หลัก + More=hamburger) — founder ชอบ hamburger แต่ยอมรับ bottom nav มาตรฐาน · park ให้คิดต่อ
- **backend เล็ก:** เปลี่ยนรหัสผ่าน, notification pref (settings แสดง "เร็วๆ นี้" ไว้)
- **light theme pass-3:** ถ้าเจอ component มืดตายตัวอื่นที่ไม่ flip (inline rgba(255,255,255) ในบาง JS template)
- AI recommended (worker dashboard) ใช้พิกัด BKK default → ทีหลังใส่พิกัด worker จริง

## 🐛 Known bugs (P2 — ไม่ block pilot, จัดการรอบ product หน้า)
- **Notification ชนกัน** — noti บางตัวชนกัน/ซ้ำ (รายละเอียดรอ founder ระบุตอนกลับมาทำ) · ไม่ block loop หลัก · deprioritize เพราะโฟกัส pilot market ก่อน

## 🧪 ควรเทสก่อน pilot จริง (บน production หลัง deploy)
1. Employer full loop: dashboard → โพสต์ → ดูผู้สมัคร/จ้างทันที → Roster (เช็คอิน→timer→verify→จ่าย)
2. Worker full loop: dashboard → Active Shift (เช็คอิน GPS→timer→จบ→ยืนยันรับเงิน)
3. Pay: จ่ายงาน → เช็ค paid_amount = ค่าจ้าง×วัน (ลอง tamper devtools → ต้องยังถูก)
4. Light/dark toggle ทุกหน้า (setting) — sepia อ่านชัด ไม่แสบตา
5. hard-refresh (Ctrl+Shift+R) กัน cache

## 🔑 Key context
- git push = founder (hook block) · session นี้ founder อนุญาต agent push ได้
- helper กลาง: `ico()` (SVG), `_hhmmss/_elapsedSec/_fmtHM` (timer), `_rosterKind/_rosterRank` (roster/dashboard reuse)
- Backend (Render) แตะจุดเดียว: `employer_pay` (main.py) — server คำนวณยอด
