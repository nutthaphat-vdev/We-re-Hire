# HANDOFF — Session 2026-07-16 (Auth + Double-Hire + Profile Redesign)

> โยนเข้า session ใหม่ได้เลย · โปรเจกต์: We're Hired (WeHire)
> Session นี้: เริ่มจาก "แก้ปุ่ม mockup" → บานเป็นปิดรู backend + ยกเครื่อง auth + รื้อโปรไฟล์ worker ทั้งหน้า
> **ทุกอย่าง deploy + push แล้ว (0 unpushed) · เทสจริงบน prod ผ่านครบ**

---

## ✅ SHIPPED (deploy prod + เทสผ่าน)

### 1. Double-hire cluster ปิดครบ — `test_double_hire.py` 6/6 บน prod
- **migration `023_job_time_range.sql`** (run บน Supabase แล้ว): function `job_occupied_range(start_date, duration, work_start, work_end) → tsrange`
  - วันเดียวปกติ / **ข้ามเที่ยงคืน = +1 วัน** / **หลายวัน = บล็อกทั้งสแปน** · เทียบด้วย `&&` (tsrange `[)` → back-to-back ไม่ชน)
- **`decide`** (`2fe2fc0`): `pg_advisory_xact_lock(worker_id)` + guard reject ถ้า worker ถือ `hired/checked_in/working` อื่นทับเวลา + auto-withdraw เปลี่ยนวันล้วน → วัน+เวลา
- **`accept_backup_offer`** (`2974d06`): เพิ่ม lock+guard+withdraw เดียวกัน — **อุดรูรั่ว double-hire ทาง backup path** (เดิมไม่มี guard)
- **`_cascade_backup_offer` + `get_backup_workers`**: กรอง `wp.is_available = TRUE` + `NOT EXISTS(active job ทับเวลา)`

### 2. Auth overhaul — `test_auth.py` 8/8 บน prod · commit `8af365f`
- **phone login**: `LoginRequest` รับ `identifier` (เบอร์/อีเมล) + `email` (legacy) + `remember` · login แยก email/phone lookup
- **remember me = 30 วัน** (`JWT_REMEMBER_MINUTES`, `create_token(remember)`)
- **timing-enum fix**: `_DUMMY_BCRYPT_HASH` รัน bcrypt หลอกเมื่อไม่เจอ user (constant-time กันเดาเบอร์)
- **phone สำหรับ Google user**: `WorkerProfileCreate/EmployerProfileCreate` รับ `phone` (soft, ไม่ hard-require) → `users.phone` · endpoint ใหม่ **`PATCH /auth/phone`** · helper `_set_user_phone` (validate `0XXXXXXXXX` + unique)
- **gate**: `apply` + `post-job` reject ถ้า `users.phone` ว่าง (contact-lock ต้องใช้)
- **PDPA**: `google_callback` บันทึก `terms_accepted_at` + `policy_version='1.0'` (implicit consent)

### 3. Worker Profile Redesign (index.html — page-workerprofile) · commits `38d2728`→`3c3c915`
> ยกดีไซน์ Gemini/`viewcard.html` ลง prod · **หน้า render ด้วย JS template — เก็บ id/onchange/i18n/pre-fill เดิมครบ**
- ช่องเบอร์ใน create/edit form + pre-fill จาก `/auth/me` (create ส่งใน POST · edit เรียก `PATCH /auth/phone`)
- **hero card** (`.wp-hero`): avatar gradient 76px, badge (pending/verify), stat card 3 ช่อง, ปุ่มแก้ไข — แยกเป็นการ์ดคนละใบกับ KYC
- **create/edit form** การ์ดสไตล์ Gemini (`.wp-edit`)
- **KYC card** ตรง `viewcard.html`: upload card + PDPA boxed + ปุ่มมีไอคอน
- **ไอคอน = inline SVG ทั้งหมด** (id-card, face-focus, shield, upload, pin, pencil, magnifier) — ⚠️ **Phosphor CDN ไม่ render ใน prod** (เดา CSP/font timing) → เลิกใช้ CDN, วาด SVG เอง render ชัวร์
- `_noIco()` strip emoji นำหน้าจาก i18n (dict มี `✏️🟢🔍📤` → ซ้ำกับ SVG) · dot สีตามสถานะ
- **KYC capture**: selfie `capture="user"` (บังคับกล้องหน้า) · บัตร = chooser (กล้อง+คลัง fallback)

---

## 🔍 AUDIT & REVIEW

### แข็งแรง / มั่นใจได้
- Double-hire: unit-test SQL 7/7 + integration 6/6 บน prod · guard ครอบ **ทั้ง 2 hire paths** (decide + accept-backup) · advisory lock กัน race
- Auth: backward-compatible 100% (frontend เก่าส่ง `{email,password}` ยังทำงาน) · parameterized ทุก query · `get_current_user` เช็ก `is_active` ทุก request (ban ตัดได้ทันที)
- Profile redesign: JS syntax ผ่าน (node --check) ทุก commit · id/handler เดิมครบ (category picker, GPS, submit ไม่พัง) · เทส create+edit จริงบน prod (เบอร์ save+pre-fill ติด)

### ⚠️ ความเสี่ยง / debt ที่ยอมรับไว้ (ยังไม่ทำ — ไม่ใช่บั๊กด่วน)
- **JWT 30 วัน revoke ไม่ได้** (stateless) — token ถูกขโมย = ใช้ได้ 30 วัน · **ต้องแก้ก่อน escrow**: เพิ่ม `token_version` ใน users, bump ตอน logout-all/เปลี่ยนรหัส
- **Rate-limit login = 10/min ต่อ IP** เท่านั้น — IP หมุนได้ · ทีหลังเพิ่ม lockout ต่อบัญชี
- **`capture` = deterrent ไม่ใช่ anti-spoof** — ยกรูปส่องกล้องได้ · กันจริง = liveness (roadmap selfie check-in)
- **backup cascade เรียงด้วย `worker_profiles.location` (stored)** — ถ้าเปลี่ยนไป real-time GPS ล้วน (แผนพี่) อันนี้ต้องปรับ
- **เบอร์ recycle** (ค่ายแจกเบอร์เก่าซ้ำ) — เกี่ยวตอนทำ OTP recovery
- **Phosphor ถูกถอด** — ถ้าจะใช้ไอคอนเพิ่มในอนาคต ใช้ inline SVG (อย่าใส่ CDN กลับ — prod ไม่ render)

### Security review (auth)
- ✅ SQL injection: phone normalize เหลือตัวเลข + parameterized · ✅ enumeration ทาง error message (ข้อความรวม) + timing (dummy bcrypt) · ✅ password bcrypt cost 12 ไม่แตะ · 🟡 revocation (ข้างบน)

---

## ⏭️ NEXT — Employer side (สำหรับ pitch โรงแรม)

**บริบท:** hotel HR 2-3 เจ้า "อยากได้ตัวเต็ม" แต่**ยังไม่ใช้** (บอกอยากให้ดีกว่านี้ก่อน) · pull อยู่ฝั่ง employer = seed ที่ถูก

**Employer mockup มีแล้ว 8 หน้าใน `GeminiDesign/*employer*.html`** (ใช้ `dashboard.css`+`employer-classic.css`):
- ค่อนข้างเสร็จ: dashboard, my-jobs, view-applicants, reviews, notifications
- ยังหยาบ (มี placeholder): **post-job (5), profile (3), live-roster (1)**

**Critical path จ้างจริง (โรงแรมแตะแค่นี้):** โพสต์งาน → ดูผู้สมัคร → กดจ้าง
- **ทำ 2 หน้าก่อนพอ:** `post-job-employer` (หยาบสุด + ด่านแรก) + `view-applicants-employer` (เกือบเสร็จ)
- **backend มีครบแล้ว** (`POST /jobs`, `get_candidates`, `decide` มี double-hire guard) → งานคือ **ยก UI สวยครอบ endpoint เดิม + wire** เหมือนที่ทำ worker profile (ไม่มี logic ใหม่)
- production ฝั่ง employer ตอนนี้บาง: มี `page-postjob`, `page-myjobs` + admin · view-applicants/roster/employer-profile ยังไม่ยกลง

**⚠️ กับดัก:** "ทำให้ดีกว่านี้ก่อน" มักเป็น polite stall → อย่าขัดครบ 8 หน้าก่อน · **ผลัก pilot: เซ็ตให้โรงแรม 1 เจ้ารันจ้างจริง 1 กะ** → เปลี่ยน "สนใจ" เป็น "ใช้อยู่"

---

## 🔑 Decisions ที่ล็อกแล้ว
| เรื่อง | สรุป |
|-------|------|
| overlap นิยาม | time-based (`tsrange &&`) · วันเดียว=เวลาทับ · หลายวัน=เต็มสแปน · ข้ามคืน=+1วัน |
| apply | อิสระ (ไม่บล็อก) — ป้องกันที่ hire อย่างเดียว |
| remember me | 30 วัน |
| phone login | เบอร์-หรือ-อีเมล + password (OTP เฟสหลัง) |
| เบอร์ Google user | เก็บผ่านฟอร์ม profile (soft) + `PATCH /auth/phone` · gate ที่ apply/post |
| KYC | ไทยก่อน (nationality default thai) · ต่างด้าว park (spec 3 ส่วน selfie+passport+work permit ใน CLAUDE.md) |
| KYC capture | selfie บังคับกล้อง · บัตร chooser |
| ไอคอน | inline SVG เท่านั้น (Phosphor CDN prod ไม่ render) |
| GPS | ตัดจากฟอร์ม profile · หางานใช้ live GPS ตอนกด (findjobs รับ lat/lng อยู่แล้ว) · real-time trigger = แผนยกเครื่อง |

## 📁 ไฟล์/commit อ้างอิง
- Backend: `main.py` (decide/backup/auth/profile-phone) · `023_job_time_range.sql` (ใหม่, run แล้ว)
- Frontend: `index.html` (auth + page-workerprofile redesign)
- Tests: `test_double_hire.py` (6 เคส) · `test_auth.py` (8 เคส, default ชี้ prod)
- Ref design: `GeminiDesign/viewcard.html`, `GeminiDesign/profile.html`
- commits session นี้: `8af365f`→`3c3c915` (11 commits) · double-hire: `2fe2fc0`,`2974d06`

## 🛠️ Tool / Working notes
- แก้ไฟล์ Hire → `Edit` · **git push สงวนให้พี่ (hook `block_git_write.py` — agent commit ได้ push ไม่ได้)**
- ⚠️ **mount Linux (`mcp__workspace__bash`) sync ช้า/torn-read บ่อย** — validate ไฟล์ใหญ่ (index.html/main.py) ให้เชื่อ **Read tool + `mcp__shell__run_command` (node/python บน Windows)** เป็นหลัก
- migration ต้อง run บน Supabase **ก่อน** push โค้ดที่เรียก function ใหม่
- frontend เทส unit ไม่ได้ → **node --check** (JS syntax) + **screenshot จากพี่** คือตัวตัดสิน visual
